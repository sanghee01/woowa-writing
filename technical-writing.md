# Sentry 에러 모니터링, 똑똑하게 활용하기

프로젝트 초기에 Sentry를 세팅했습니다. 에러가 발생하면 Sentry에 잘 쌓이는 것도 확인했습니다. 하지만 솔직히 말하면, 그게 전부였습니다. Sentry 대시보드에 들어가면 에러들이 쌓여있긴 한데, 정확히 뭘 봐야 할지 모르겠고, 봐도 어떤 에러인지 파악하기 어려웠습니다. 그냥 방치하고 있었죠.

그러다 우리 서비스를 직접 사용하면서 몇 가지 에러를 발견했습니다. 특정 버튼을 클릭했을 때 아무 반응이 없거나, 페이지에 들어갔는데 무한 로딩이 생기거나, 기능이 의도대로 동작하지 않는 경우가 있었어요. "내가 이렇게 발견하는데, 다른 사용자들도 이런 에러를 겪고 있지 않을까?" 하는 생각이 들어 오랜만에 Sentry에 들어갔습니다. 역시나 에러가 몇 개 쌓여있었지만, 어떤 걸 봐야 할지 모르겠어서 그냥 쓱 구경만 하고 나왔습니다.

며칠간 직접 서비스를 사용하고, 다른 팀원들의 사용 모습을 지켜보면서 깨달았습니다. 사소한 에러라도, 에러가 발생한 것만으로도 사용자의 신뢰도를 떨어뜨릴 수 있고, 사용자 이탈로 이어질 수 있다는 것을요.

그래서 방치되고 있던 Sentry 에러 모니터링을 더 똑똑하고 잘 활용하기 위해 모니터링 개선 작업을 진행했습니다. 이 글에서는 에러 모니터링 개선 과정에서 알게 된 인사이트와, Sentry를 효과적으로 활용하는 방법을 공유하고자 합니다.

---

## 문제 상황

Sentry의 Issues 페이지에 들어가면 에러 통계, 브라우저/OS 정보, Stack Trace 등 기본적인 정보를 제공합니다. 하지만 실제로 사용하면서 두 가지 문제가 명확했습니다.

첫 번째는 난독화된 Stack Trace입니다. 프로덕션 환경에서 코드가 번들링되고 압축되면, Stack Trace에 `bundle.js:1:234567` 같은 형태로만 나타납니다. 정확히 어느 파일의 어느 함수에서 에러가 발생했는지 알 수 없으니, 매번 소스맵을 수동으로 확인해야 구체적인 코드를 확인할 수 있었습니다.

두 번째는 부족한 컨텍스트입니다. URL이 /posts라고 해도 게시글 목록을 불러오다가 발생한 건지, 작성하다가 발생한 건지, 삭제하다가 발생한 건지 알 수 없었습니다. 사용자가 어떤 행동을 했을 때 에러가 발생했는지, 그때 어떤 데이터를 다루고 있었는지 같은 정보가 없으니 원인을 찾기가 정말 어려웠습니다.

이러한 문제들을 해결하기 위해 팀 프로젝트에 바로 적용해볼 수도 있었지만, 팀 프로젝트 특성상 PR과 코드 리뷰 과정을 거쳐야 하고, 실험적인 시도를 하기에는 부담스러웠습니다. 그래서 우리 프로젝트 환경과 최대한 비슷한 미니 프로젝트를 만들어서, 하나하나 실험하며 적용해보기로 했습니다. 이렇게 하면 빠르게 시도하고 결과를 확인할 수 있고, 실패해도 팀에 영향을 주지 않으며, 검증된 방법만 팀 프로젝트에 적용할 수 있기 때문입니다.

---

## 프로젝트 환경 구축 및 기본 세팅

실험을 위해 React 19, TypeScript, Webpack을 사용하는 환경을 구축했습니다. Axios로 HTTP 통신을 처리하고, JSON Server로 Mock API를 만들었습니다. 다른 프레임워크, 라이브러리를 사용하더라도 전체적인 흐름은 비슷하므로 기술 스택은 참고만 하시면 됩니다. 중요한 건 어떻게 Sentry를 효과적으로 활용하는가입니다.

Sentry 공식 문서를 기반으로 기본 세팅을 진행했습니다. 먼저 애플리케이션이 시작될 때 Sentry를 초기화합니다.

```typescript
import * as Sentry from "@sentry/react";

export function initSentry() {
  const dsn = process.env.REACT_APP_SENTRY_DSN;
  const environment = process.env.REACT_APP_SENTRY_ENVIRONMENT || "development";

  if (!dsn || dsn === "YOUR_SENTRY_DSN_HERE") {
    console.warn("⚠️ Sentry DSN이 설정되지 않았습니다.");
    return;
  }

  Sentry.init({
    dsn,
    environment,

    integrations: [
      Sentry.browserTracingIntegration(),
      Sentry.replayIntegration({
        maskAllText: false,
        blockAllMedia: false,
      }),
    ],

    tracesSampleRate: environment === "development" ? 1.0 : 0.1,
    replaysSessionSampleRate: 0.1,
    replaysOnErrorSampleRate: 1.0,

    beforeSend(event, hint) {
      if (environment === "development") {
        console.log("📤 Sentry에 에러 전송:", event);
      }

      const error = hint.originalException;
      if (error && error.toString().includes("ResizeObserver")) {
        return null;
      }

      return event;
    },

    debug: environment === "development",
  });

  console.log("✅ Sentry 초기화 완료");
}
```

DSN은 Sentry 프로젝트를 식별하는 고유 주소입니다. environment는 현재 환경을 구분하는데, development, staging, production 등으로 설정하면 Sentry 대시보드에서 환경별로 에러를 필터링할 수 있습니다. tracesSampleRate는 성능 추적 샘플링 비율로, 개발 환경에서는 1.0으로 설정해 모든 트랜잭션을 추적하고, 프로덕션에서는 0.1로 설정해 비용과 성능을 고려해 일부만 추적합니다.

replaysSessionSampleRate와 replaysOnErrorSampleRate는 세션 리플레이 샘플링 비율입니다. 일반 세션은 10%만 녹화해서 비용을 절감하고, 에러가 발생한 세션은 100% 녹화해서 에러 재현을 위한 정보를 충분히 수집합니다. beforeSend는 에러를 Sentry로 보내기 전에 실행되는 함수로, 여기서 특정 에러를 필터링하거나 추가 정보를 넣을 수 있습니다.

다음으로 Axios 인터셉터를 사용해서 API 요청 중 발생하는 에러를 Sentry로 전송하도록 설정했습니다.

```typescript
import axios, { AxiosError } from "axios";
import * as Sentry from "@sentry/react";

export const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL || "http://localhost:4000",
  timeout: 10000,
  headers: {
    "Content-Type": "application/json",
  },
});

api.interceptors.response.use(
  (response) => response,
  (error: AxiosError) => {
    console.error("[API Response Error]", error);

    let errorMessage = "알 수 없는 에러가 발생했습니다.";
    let errorLevel: "error" | "warning" = "error";

    if (error.response) {
      const status = error.response.status;
      switch (status) {
        case 400:
          errorMessage = "잘못된 요청입니다.";
          errorLevel = "warning";
          break;
        case 401:
          errorMessage = "인증이 필요합니다.";
          errorLevel = "warning";
          break;
        case 403:
          errorMessage = "권한이 없습니다.";
          errorLevel = "warning";
          break;
        case 404:
          errorMessage = "요청한 리소스를 찾을 수 없습니다.";
          errorLevel = "warning";
          break;
        case 500:
          errorMessage = "서버 에러가 발생했습니다.";
          break;
        default:
          errorMessage = "알 수 없는 에러가 발생했습니다.";
      }
    } else if (error.request) {
      errorMessage = "네트워크 에러가 발생했습니다. 서버에 연결할 수 없습니다.";
    } else {
      errorMessage = `요청 중 에러가 발생했습니다: ${error.message}`;
    }

    Sentry.captureException(error, {
      level: errorLevel,
      tags: {
        errorType: "api_error",
      },
      contexts: {
        api: {
          url: error.config?.url || "unknown",
          method: error.config?.method || "unknown",
          status: error.response?.status || "no response",
        },
      },
      extra: {
        errorMessage,
        requestData: error.config?.data,
        responseData: error.response?.data,
      },
    });

    return Promise.reject(error);
  }
);
```

Axios 에러는 크게 세 가지 타입으로 구분됩니다. error.response가 있으면 서버가 응답을 보냈지만 상태 코드가 2xx가 아닌 경우고, error.request만 있으면 요청은 보냈지만 응답을 받지 못한 경우, 그 외는 요청 설정 중에 에러가 발생한 경우입니다. 400번대 에러는 클라이언트 측 문제이므로 warning으로, 500번대 에러는 서버 측 문제이므로 error로 레벨을 설정했습니다.

tags는 에러를 분류하기 위한 태그로 나중에 필터링할 때 사용하고, contexts는 구조화된 추가 정보로 API URL, HTTP 메서드, 상태 코드 등을 담습니다. extra는 기타 추가 정보로 요청 데이터나 응답 데이터 등을 기록합니다.

React 컴포넌트에서 발생하는 렌더링 에러를 잡기 위해 Error Boundary도 구현했습니다.

```typescript
import { Component, ReactNode, ErrorInfo } from "react";
import * as Sentry from "@sentry/react";

interface ErrorBoundaryProps {
  children: ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = {
      hasError: false,
      error: null,
    };
  }

  static getDerivedStateFromError(error: Error): Partial<ErrorBoundaryState> {
    return {
      hasError: true,
      error,
    };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
    console.error("ErrorBoundary caught an error:", error, errorInfo);

    Sentry.captureException(error, {
      level: "error",
      tags: {
        errorType: "render_error",
        errorBoundary: true,
      },
      contexts: {
        react: {
          componentStack: errorInfo.componentStack,
        },
      },
    });
  }

  handleReset = (): void => {
    this.setState({
      hasError: false,
      error: null,
    });
  };

  render(): ReactNode {
    const { hasError, error } = this.state;
    const { children } = this.props;

    if (hasError) {
      return (
        <div style={{ padding: "2rem", textAlign: "center" }}>
          <h1 style={{ color: "#dc3545" }}>⚠️ 에러가 발생했습니다</h1>
          <p>죄송합니다. 예상치 못한 오류가 발생했습니다.</p>
          <button onClick={this.handleReset}>다시 시도</button>

          {process.env.NODE_ENV === "development" && error && (
            <details style={{ marginTop: "2rem" }}>
              <summary>에러 상세 정보</summary>
              <pre>{error.toString()}</pre>
            </details>
          )}
        </div>
      );
    }

    return children;
  }
}

export default ErrorBoundary;
```

다만 Error Boundary는 이벤트 핸들러 내부의 에러, 비동기 코드의 에러, 서버 사이드 렌더링 에러, Error Boundary 자체에서 발생한 에러는 잡을 수 없습니다. 따라서 이벤트 핸들러나 비동기 코드에서 발생하는 에러는 별도로 처리해야 합니다.

세팅이 완료되었으니 실제로 에러를 발생시켜서 Sentry에 잘 전송되는지 확인했습니다. 렌더링 에러, API 에러, 네트워크 에러 등을 테스트했고, Sentry 대시보드에 에러가 잘 쌓이는 것을 확인할 수 있었습니다.

---

## 소스맵을 활용한 정확한 에러 추적

프로덕션 환경에서는 파일 크기 감소, 네트워크 전송량 감소, 코드 난독화 등의 이유로 코드를 번들링하고 압축합니다. 하지만 이렇게 압축된 코드는 사람이 읽기 어렵습니다. 원본 코드에서는 `calculateTotal`이라는 명확한 함수명이 있었는데, 압축 후에는 `a` 같은 한 글자 변수명으로 바뀝니다. 에러가 발생했을 때 Stack Trace에 `bundle.js:1:234567`라고 나오면, 원본 코드의 어디에서 에러가 발생한 건지 알 수 없습니다.

소스맵은 번들링되고 압축된 코드를 원본 코드로 매핑해주는 파일입니다. 이를 사용하면 압축된 코드의 위치를 원본 코드의 위치로 변환할 수 있습니다. 소스맵을 사용하면 에러 발생 위치를 정확하게 파악할 수 있고, 디버깅 시간이 크게 단축되며, 원본 코드를 볼 수 있어 에러 원인 파악이 쉬워집니다.

다만 소스맵 파일 크기가 크고, 소스맵을 공개하면 원본 코드가 노출될 수 있다는 단점이 있습니다. 이를 해결하기 위해 소스맵 파일을 Sentry에만 업로드하고, 프로덕션 서버에는 배포하지 않습니다. 이렇게 하면 사용자는 소스맵을 다운로드할 수 없지만, Sentry는 소스맵을 사용해서 에러 위치를 정확하게 표시할 수 있습니다.

Webpack 설정에서 devtool 옵션을 사용해 소스맵을 생성합니다.

```javascript
export default (env, argv) => {
  const isDevelopment = argv.mode === "development";

  return {
    devtool: isDevelopment ? "eval-source-map" : "source-map",
  };
};
```

개발 환경에서는 eval-source-map을 사용합니다. 빠른 리빌드 속도와 정확한 소스맵을 제공하기 때문입니다. 개발 중에는 빌드 속도가 중요하므로 이 옵션을 사용합니다. 프로덕션 환경에서는 source-map을 사용합니다. 별도의 .map 파일을 생성하고 가장 정확한 소스맵을 제공합니다. Sentry에 업로드하기 위해 이 옵션을 사용합니다.

소스맵을 Sentry에 업로드하는 방법은 여러 가지가 있는데, 가장 간단한 방법은 Sentry Webpack Plugin을 사용하는 것입니다.

```bash
npm install --save-dev @sentry/webpack-plugin
```

Webpack 설정에 플러그인을 추가합니다.

```javascript
import { sentryWebpackPlugin } from "@sentry/webpack-plugin";

export default (env, argv) => {
  const isDevelopment = argv.mode === "development";

  return {
    entry: "./src/index.tsx",
    output: {
      path: path.resolve(__dirname, "dist"),
      filename: "bundle.js",
      clean: true,
      publicPath: "/",
    },

    devtool: isDevelopment ? "eval-source-map" : "source-map",

    plugins: [
      ...(!isDevelopment
        ? [
            sentryWebpackPlugin({
              org: "your-org-name",
              project: "your-project-name",
              authToken: process.env.SENTRY_AUTH_TOKEN,
            }),
          ]
        : []),
    ],
  };
};
```

Sentry Auth Token은 대시보드에서 Settings, Account, API, Auth Tokens로 이동해서 발급받을 수 있습니다. Create New Token을 클릭하고, Scopes에서 project:read, project:releases, org:read 권한을 선택합니다. 생성된 토큰은 .env 파일에 저장하고, 절대 Git에 커밋하지 않도록 주의해야 합니다.

프로덕션 빌드를 실행하면 Sentry Webpack Plugin이 자동으로 생성된 소스맵 파일을 찾아서 Sentry에 업로드하고, 로컬의 소스맵 파일을 삭제합니다. 이제 에러를 발생시켜보면, Sentry 대시보드에서 원본 코드를 볼 수 있습니다. 소스맵 적용 전에는 `bundle.js:1:234567` 같은 형태로 나타났지만, 적용 후에는 `handleApiError (src/pages/PostsPage.tsx:42:15)` 같은 형태로 정확한 파일명과 라인 번호를 볼 수 있습니다.

---

## 태그와 컨텍스트로 에러 정보 구조화하기

기본 Sentry 세팅만으로는 어떤 페이지나 컴포넌트에서 에러가 발생했는지, 사용자가 어떤 행동을 했을 때 에러가 발생했는지, 에러 발생 당시 애플리케이션의 상태는 어땠는지를 알기 어렵습니다. 예를 들어, `/posts` 페이지에서 에러가 발생했다고 해도 게시글 목록을 불러오다가 에러가 발생한 건지, 게시글을 작성하다가 에러가 발생한 건지, 게시글을 삭제하다가 에러가 발생한 건지 명확하지 않습니다.

이 문제를 해결하기 위해 Sentry Context 시스템을 만들었습니다. 이 시스템은 현재 컴포넌트와 사용자 행동에 대한 정보를 저장하고, 에러가 발생했을 때 이 정보를 Sentry에 함께 전송합니다.

먼저 SentryContext 유틸리티 클래스를 만들었습니다.

```typescript
interface SentryContextData {
  component?: string;
  feature?: string;
  action?: string;
  additionalData?: Record<string, unknown>;
}

class SentryContext {
  private static context: SentryContextData = {};

  static setContext(data: SentryContextData): void {
    this.context = { ...this.context, ...data };
  }

  static setComponent(component: string): void {
    this.context.component = component;
  }

  static setFeature(feature: string): void {
    this.context.feature = feature;
  }

  static setAction(action: string): void {
    this.context.action = action;
  }

  static setAdditionalData(data: Record<string, unknown>): void {
    this.context.additionalData = { ...this.context.additionalData, ...data };
  }

  static getContext(): SentryContextData {
    return this.context;
  }

  static clear(): void {
    this.context = {};
  }
}

export default SentryContext;
```

이 클래스는 현재 컴포넌트 이름, 기능, 사용자 행동, 추가 데이터를 저장하고 조회하는 메서드를 제공합니다. 매번 클래스 메서드를 직접 호출하는 것은 불편하므로, 커스텀 훅으로 만들어서 편리하게 사용할 수 있도록 했습니다.

```typescript
import { useEffect } from "react";
import SentryContext from "../lib/sentry-context";

interface UseSentryContextOptions {
  component?: string;
  feature?: string;
  action?: string;
  additionalData?: Record<string, unknown>;
}

export function useSentryContext(options: UseSentryContextOptions) {
  useEffect(() => {
    if (options.component) {
      SentryContext.setComponent(options.component);
    }
    if (options.feature) {
      SentryContext.setFeature(options.feature);
    }
    if (options.action) {
      SentryContext.setAction(options.action);
    }
    if (options.additionalData) {
      SentryContext.setAdditionalData(options.additionalData);
    }

    return () => {
      SentryContext.clear();
    };
  }, [
    options.component,
    options.feature,
    options.action,
    options.additionalData,
  ]);

  return {
    setAction: (action: string) => SentryContext.setAction(action),
    setAdditionalData: (data: Record<string, unknown>) =>
      SentryContext.setAdditionalData(data),
  };
}
```

컴포넌트가 마운트될 때 컨텍스트를 설정하고, 언마운트될 때 컨텍스트를 초기화합니다. 또한 액션 설정 함수를 반환해서 이벤트 핸들러에서 사용할 수 있도록 했습니다.

실제 사용 예시를 보면 이렇습니다.

```typescript
import { useSentryContext } from "../hooks/useSentryContext";
import SentryContext from "../lib/sentry-context";

function PostsPage() {
  const { setAction, setAdditionalData } = useSentryContext({
    component: "PostsPage",
    feature: "게시글 관리",
  });

  const handleCreatePost = async () => {
    setAction("게시글 작성 버튼 클릭");

    try {
      const newPost = await api.post("/posts", {
        title: "새 게시글",
        content: "내용",
      });

      setAdditionalData({ postId: newPost.data.id });
    } catch (error) {
      console.error("게시글 작성 실패", error);
    }
  };

  const handleDeletePost = async (postId: number) => {
    setAction("게시글 삭제 버튼 클릭");
    setAdditionalData({ postId });

    try {
      await api.delete(`/posts/${postId}`);
    } catch (error) {
      console.error("게시글 삭제 실패", error);
    }
  };

  return (
    <div>
      <button onClick={handleCreatePost}>게시글 작성</button>
      <button onClick={() => handleDeletePost(123)}>게시글 삭제</button>
    </div>
  );
}
```

페이지에 진입하면 자동으로 컴포넌트 이름과 기능이 설정됩니다. 사용자가 버튼을 클릭할 때마다 setAction을 호출해서 현재 사용자가 어떤 행동을 하고 있는지 기록합니다. 에러가 발생하면 현재 설정된 컨텍스트 정보가 자동으로 Sentry에 함께 전송됩니다.

이제 Axios 인터셉터를 수정해서 에러 발생 시 컨텍스트 정보를 함께 전송하도록 했습니다.

```typescript
api.interceptors.response.use(
  (response) => response,
  (error: AxiosError) => {
    const sentryContext = SentryContext.getContext();

    let errorMessage = "알 수 없는 에러가 발생했습니다.";
    let errorLevel: "error" | "warning" = "error";

    if (error.response) {
      const status = error.response.status;
      if (status >= 400 && status < 500) {
        errorLevel = "warning";
        errorMessage =
          {
            400: "잘못된 요청입니다.",
            401: "인증이 필요합니다.",
            403: "권한이 없습니다.",
            404: "요청한 리소스를 찾을 수 없습니다.",
          }[status] || "클라이언트 에러가 발생했습니다.";
      } else if (status >= 500) {
        errorMessage = "서버 에러가 발생했습니다.";
      }
    } else if (error.request) {
      errorMessage = "네트워크 에러가 발생했습니다.";
    } else {
      errorMessage = `요청 중 에러가 발생했습니다: ${error.message}`;
    }

    Sentry.captureException(error, {
      level: errorLevel,
      tags: {
        errorType: "api_error",
        component: sentryContext.component,
        feature: sentryContext.feature,
        action: sentryContext.action,
      },
      contexts: {
        api: {
          url: error.config?.url || "unknown",
          method: error.config?.method || "unknown",
          status: error.response?.status || "no response",
        },
        feature: sentryContext,
      },
      extra: {
        errorMessage,
        requestData: error.config?.data,
        responseData: error.response?.data,
        ...sentryContext.additionalData,
      },
    });

    return Promise.reject(error);
  }
);
```

컨텍스트 정보를 가져와서 tags에 추가합니다. tags는 Sentry 대시보드에서 필터링할 때 사용되므로, 어떤 컴포넌트에서 어떤 기능을 수행하다가 어떤 행동 중에 에러가 발생했는지 한눈에 파악할 수 있습니다. 또한 전체 컨텍스트 데이터를 contexts에 넣고, 추가 데이터를 extra에 넣어서 구조화된 정보를 제공합니다.

이제 Sentry 대시보드에서 에러를 확인하면 tags에 component, feature, action이 보입니다. Contexts 섹션에서는 어떤 API를 호출했는지, 어떤 HTTP 메서드를 사용했는지, 어떤 상태 코드를 받았는지 볼 수 있습니다. Extra 섹션에서는 에러 메시지, 요청 데이터, 응답 데이터, 그리고 추가로 저장한 데이터들을 확인할 수 있습니다. 이제 에러가 발생했을 때 컴포넌트, 기능, 행동 정보가 자동으로 기록되기 때문에 "PostsPage에서 게시글 삭제하려다가 404 에러가 났구나. postId는 123번이었고..." 하는 식으로 빠르게 상황을 파악할 수 있습니다.

---

## Session Replay로 사용자 행동 재현하기

에러 로그만으로는 알기 어려운 것들이 많습니다. 사용자가 정확히 어떤 순서로 행동했는지, 어떤 버튼을 클릭했는지, 어떤 입력값을 넣었는지, 화면이 어떻게 변화했는지 말이죠. 개발자도 "내 환경에서는 재현이 안 되는데요" 같은 상황을 자주 겪습니다. 사용자의 환경, 브라우저, 네트워크 상태 등을 정확히 모르니까요.

Session Replay는 에러가 발생하기 전까지 사용자의 행동을 영상처럼 재현해주는 기능입니다. 에러가 발생하기까지의 전체 과정을 볼 수 있으니, 에러 재현과 디버깅이 훨씬 쉬워집니다.

Session Replay는 Sentry 초기화 시 replayIntegration을 추가하면 활성화됩니다.

```typescript
Sentry.init({
  dsn,
  environment,

  integrations: [
    Sentry.browserTracingIntegration(),

    Sentry.replayIntegration({
      maskAllText: false,
      blockAllMedia: false,
    }),
  ],

  replaysSessionSampleRate: 0.1,
  replaysOnErrorSampleRate: 1.0,
});
```

maskAllText는 텍스트 마스킹 여부를 결정합니다. true로 설정하면 모든 텍스트가 별표로 표시됩니다. blockAllMedia는 이미지나 비디오를 차단할지 결정하는데, 이것도 true로 설정하면 미디어가 녹화되지 않습니다.

샘플링 비율 설정이 중요합니다. Session Replay는 영상 데이터를 저장하기 때문에 비용이 많이 듭니다. replaysSessionSampleRate는 일반 세션을 10%만 녹화해서 비용을 절감합니다. 사용자 행동 패턴을 분석하는 용도로 일부만 녹화하면 충분합니다. replaysOnErrorSampleRate는 에러가 발생한 세션을 100% 녹화합니다. 에러 디버깅을 위해서는 에러 발생 세션의 모든 정보를 수집해야 하니까요.

개인정보 보호도 고려해야 합니다. 사용자 행동을 녹화하다 보니 이메일이나 비밀번호 같은 민감한 정보가 노출될 수 있습니다. maskAllText를 true로 설정하면 모든 텍스트를 마스킹할 수 있지만, 그러면 디버깅이 어려워집니다. 차라리 특정 요소만 선택적으로 마스킹하는 게 낫습니다.

```typescript
Sentry.replayIntegration({
  maskAllText: false,
  block: [".sensitive-data"],
});
```

민감한 정보를 포함하는 요소에 sensitive-data 클래스를 추가하면, 그 요소는 녹화되지 않습니다.

```tsx
<input
  type='email'
  className='sensitive-data'
  value={email}
  onChange={(e) => setEmail(e.target.value)}
/>
```

이렇게 하면 사용자의 이메일은 입력되지만 Session Replay에는 기록되지 않습니다.

Sentry 대시보드에서 에러를 클릭하고 Replays 탭을 선택하면 사용자의 행동을 영상처럼 볼 수 있습니다. 사용자가 어떤 페이지에서 시작했는지, 어떤 버튼을 클릭했는지, 어떤 텍스트를 입력했는지, 에러가 발생하기 직전에 뭘 했는지 모두 볼 수 있습니다. 영상 플레이어처럼 일시정지, 되감기, 빨리감기도 가능합니다. 또한 네트워크 요청 타임라인, 콘솔 로그, DOM 변경 사항, 사용자 인터랙션 같은 추가 정보도 함께 볼 수 있습니다.

"내 환경에서는 재현이 안 되는데요" 문제가 사라집니다. 영상으로 보면 답이 보이거든요. 사용자가 어떤 환경에서, 어떤 순서로 행동했는지 명확하게 알 수 있으니 디버깅이 훨씬 빨라집니다.

---

## 성능 모니터링과 Trace 활용하기

에러 모니터링을 개선하면서 성능 모니터링도 함께 보니 훨씬 유용했습니다. 에러와 성능은 연결되어 있기 때문입니다. 느린 API가 타임아웃 에러로 이어지기도 하고, 특정 페이지가 느리게 로드되면 사용자가 새로고침을 반복하다가 에러가 발생하기도 합니다. 메모리 누수가 쌓이다가 결국 크래시로 이어지기도 하죠. 성능 문제를 미리 발견하면 에러를 예방할 수 있습니다.

Performance Monitoring은 Sentry 초기화 시 browserTracingIntegration을 추가하면 활성화됩니다.

```typescript
Sentry.init({
  dsn,
  environment,

  integrations: [Sentry.browserTracingIntegration()],

  tracesSampleRate: 0.1,
});
```

이렇게 하면 자동으로 페이지 로드 시간, API 요청 시간, 컴포넌트 렌더링 시간 같은 것들을 추적합니다.

Trace는 하나의 작업을 구성하는 모든 하위 작업들을 추적하는 기능입니다. 예를 들어 `/posts` 페이지를 로드할 때, HTML 다운로드, JavaScript 다운로드, React hydration, API 요청들, 이미지 로딩 같은 다양한 작업들이 동시에 또는 순차적으로 발생합니다. Trace를 보면 이 모든 작업의 시간을 측정할 수 있고, 어느 부분이 병목인지 파악할 수 있습니다.

Sentry는 각 Trace에 고유한 ID를 부여합니다. 에러가 발생하면 이 Trace ID도 함께 기록됩니다. 예를 들어 사용자가 `/posts` 페이지에 접속해서 여러 API 요청이 발생한 후 그 중 하나가 실패해서 에러가 발생했다면, 그 에러에는 해당 Trace의 ID가 포함됩니다. Sentry에서 에러를 클릭하면 해당 Trace의 전체 타임라인을 확인할 수 있습니다. 에러만 보면 "API 요청이 실패했구나" 정도만 알 수 있지만, Trace를 보면 "아, 이미 3개 API가 호출된 상태에서 4번째 API가 타임아웃으로 실패했구나" 같은 맥락을 파악할 수 있습니다.

자동으로 추적되지 않는 작업도 수동으로 추적할 수 있습니다.

```typescript
import * as Sentry from "@sentry/react";

async function processLargeData(data) {
  return await Sentry.startSpan(
    { name: "대용량 데이터 처리", op: "process" },
    async () => {
      const filtered = data.filter(item => /* ... */);
      const sorted = filtered.sort(/* ... */);
      const grouped = groupBy(sorted, "category");
      return grouped;
    }
  );
}
```

이렇게 하면 "대용량 데이터 처리" 작업이 Trace에 표시되고, 얼마나 걸렸는지 볼 수 있습니다.

성능 모니터링을 활용한 실제 개선 사례가 있습니다. `/api/posts` 요청이 평균 800ms 걸리는 것을 발견했고, 이를 백엔드 팀에 공유했습니다. 그들이 인덱스를 추가해서 200ms로 단축시켰습니다. 특정 컴포넌트의 렌더링 시간이 비정상적으로 길었는데, React DevTools Profiler로 확인해보니 불필요한 재렌더링이 일어나고 있었습니다. useMemo를 추가해서 해결했습니다. 이미지 로딩이 페이지 로드 시간의 50%를 차지한다는 것을 발견했고, WebP 포맷과 lazy loading을 적용해서 로딩 시간을 절반으로 줄였습니다.

성능 모니터링 덕분에 사용자가 불만을 제기하기 전에 이런 문제들을 발견하고 개선할 수 있었습니다.

---

## 실전 운영 노하우

Sentry를 효과적으로 활용하기 위해 실전 팁들을 공유합니다.

먼저 에러 레벨을 어떻게 구분할지 정하는 것이 중요합니다. Sentry는 5가지 레벨을 제공합니다. fatal은 앱 크래시나 메모리 부족 같은 서비스 전체가 멈추는 상황일 때 사용합니다. error는 API 500 에러나 데이터 저장 실패 같은 기능이 동작하지 않는 경우에 사용합니다. warning은 API 400 에러나 deprecated 함수 사용 같은 문제는 있지만 우회 가능한 경우에 사용합니다. info는 결제 완료나 회원가입 같은 중요한 이벤트를 기록할 때 사용합니다. debug는 프로덕션에서는 거의 사용하지 않고 개발 중 디버깅용으로만 사용합니다.

초반에 Sentry를 열면 에러가 너무 많아서 어디서부터 봐야 할지 모르겠습니다. 중요한 에러와 중요하지 않은 에러를 구분하는 필터링이 필요합니다. ResizeObserver 에러나 브라우저 버그 같은 것들은 우리가 해결할 수 없으니 필터링해서 무시할 수 있습니다. 브라우저 확장 프로그램에서 발생하는 에러도 필터링할 수 있습니다. 사용자가 요청을 취소한 경우도 기록할 필요가 없으니 필터링합니다.

```typescript
beforeSend(event, hint) {
  const error = hint.originalException;

  if (error?.toString().includes("ResizeObserver")) {
    return null;
  }

  if (event.exception?.values?.[0]?.stacktrace?.frames?.some(
    frame => frame.filename?.includes("chrome-extension://")
  )) {
    return null;
  }

  return event;
}
```

Alert 설정도 전략이 필요합니다. 에러가 발생할 때마다 알림을 받으면 피곤합니다. 중요한 것만 즉시 알림 받고, 나머지는 주기적으로 확인하는 게 좋습니다. Slack으로 즉시 알림을 받도록 설정할 것은 level이 fatal인 에러, 10분 동안 같은 에러가 50회 이상 발생한 경우(급증), 결제나 로그인 같은 핵심 기능 에러입니다. 이메일로 일일 요약을 받도록 설정할 것은 처음 발생한 에러, 재발한 에러, 에러 트렌드 리포트입니다.

에러 우선순위를 정하는 것도 중요합니다. 모든 에러를 동시에 고칠 수는 없으니까요. 우선순위는 빈도와 심각도로 판단합니다. 높은 빈도와 높은 심각도라면 최우선으로 해결해야 합니다. 높은 심각도지만 낮은 빈도라면 빠른 시일 내에 해결합니다. 낮은 심각도지만 높은 빈도라면 개선이 필요합니다. 낮은 빈도와 낮은 심각도라면 모니터링하면서 지켜봅니다.

Release를 추적하면 어떤 버전에서 에러가 발생했는지 알 수 있습니다. "v1.2.3 배포 후 에러가 급증했네?" 같은 패턴을 발견할 수 있고, 문제가 있는 버전을 빠르게 롤백할 수 있으며, 에러 수정 후 실제로 해결됐는지 확인할 수 있습니다.

```typescript
Sentry.init({
  dsn,
  environment,
  release: `my-app@${process.env.REACT_APP_VERSION}`,
});
```

비용 최적화도 고려해야 합니다. Sentry는 이벤트 수에 따라 비용이 발생합니다. 샘플링 비율을 조정하면 비용을 줄릴 수 있습니다. 개발 환경에서는 100% 추적하지만, 프로덕션에서는 10% 정도만 추적합니다. 같은 에러가 100번 발생해도 1개 이슈로 카운트되므로 반복되는 에러는 비용 걱정 없습니다. 개발 환경에서는 Sentry에 에러를 보내지 않아서 비용을 절약할 수 있습니다.

Error Boundary 사용할 때 주의할 점이 있습니다. Error Boundary는 렌더링 에러만 잡을 수 있습니다. 이벤트 핸들러 내부의 에러, 비동기 코드의 에러, 서버 사이드 렌더링 에러는 잡을 수 없습니다. 이런 경우는 try-catch나 Promise catch로 별도로 처리해야 합니다.

```typescript
function MyComponent() {
  const handleClick = () => {
    try {
      dangerousOperation();
    } catch (error) {
      Sentry.captureException(error);
      alert("에러가 발생했습니다");
    }
  };

  return <button onClick={handleClick}>클릭</button>;
}
```

---

## 마무리

이 글은 Sentry를 처음 도입했을 때의 막막함에서 시작해서, 단계별로 개선해나가면서 배운 것들을 정리한 것입니다. 소스맵만 올려도 에러 추적이 훨씬 쉬워지고, 컨텍스트 정보를 추가하면 에러의 상황을 더 빠르게 파악할 수 있습니다. Session Replay를 사용하면 에러 재현이 쉬워지고, 성능 모니터링을 함께 보면 에러를 예방할 수도 있습니다.

무엇보다 중요한 것은 도구를 설치만 하는 게 아니라, 실제로 사용하는 것입니다. 매일 에러를 확인하고, 팀과 공유하고, 함께 개선해나가는 문화를 만드는 것이 가장 큰 차이를 만듭니다. 작은 것부터 시작해서 하나씩 개선해나가면, 어느 순간 에러 모니터링이 개발 과정에서 빼놓을 수 없는 부분이 되어 있을 겁니다.
